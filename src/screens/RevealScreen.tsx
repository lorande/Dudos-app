import React from 'react';
import { View } from 'react-native';

// Placeholder — revelação acontece via RevealOverlay dentro do GameScreen
export default function RevealScreen() {
  return <View />;
}
